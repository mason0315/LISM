spring:
  application:
    name: suggestion-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        namespace: public
      discovery:
        server-addr: localhost:8848
        namespace: public
